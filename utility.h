#ifndef UTILITY_H
#define UTILITY_H
#include <string>
#include <vector>
#include <sstream>
using namespace std;

void split_string(string input, vector<string> &target_vec);
vector<string> split_string(string input);

#endif
